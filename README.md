## TIER IV universe / split mirror (planning & control)

This repository split mirrors `main` branch of `autowarefoundation/autoware_universe`, `planning/` and `control/` directories, into `awf-latest` branch, used for TIER IV release workflows.

This branch only holds workflow files. See other branches for source codes.
